<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.base.module.pack"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- android:sharedUserId="android.uid.system" -->
    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="19" />

    <!-- ==================permission============================ -->
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" >
    </uses-permission>
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CLEAR_APP_CACHE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- ===============application===================== -->
    <application
        android:icon="@drawable/gs_market"
        android:label="@string/appname"
        android:theme="@style/AppTheme" >

        <!-- android:theme="@android:style/Theme.Holo.Light" > -->
        <!-- ==========activity============ -->
        <activity
            android:name=".main.AppActivity"
            android:launchMode="singleInstance"
            android:theme="@style/MainThem" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="market" />
            </intent-filter>

        </activity>
        <activity
            android:name=".main.ExpandActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="landscape" >
        </activity>
        <activity
            android:name=".main.AppDetialActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar" >
        </activity>
        <activity
            android:name=".main.DetailActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="landscape" >
        </activity>

        <!-- ===============receiver============ -->
        <receiver android:name=".service.DownReceiver" >

            <!-- receive -->
            <intent-filter>
                <action android:name="com.base.module.pack.intent.action.DOWN_ACTION" />
            </intent-filter>
        </receiver>
        <receiver android:name=".service.GetBroadcast" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>

            <!-- receive -->
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- ============service=============== -->
        <!-- Service -->
        <service
            android:name=".service.DownService"
            android:exported="true" />

        <provider
            android:name=".provider.PackProvider"
            android:authorities="com.base.module.pack"
            android:multiprocess="true" />
    </application>

</manifest>